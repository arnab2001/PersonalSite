# Supabase Configuration
# Get these values from your Supabase project settings (https://supabase.com/dashboard)

# Project URL - found in Settings > API
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url

# Anon (public) key - found in Settings > API > Project API keys
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Service role key (KEEP SECRET - only for sync script, never expose to client)
# Found in Settings > API > Project API keys
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Site URL (for sitemap, metadata, etc.)
NEXT_PUBLIC_SITE_URL=http://localhost:3000
